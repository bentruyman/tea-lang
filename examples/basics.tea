use debug = "std.debug"

const language = "tea-lang"
debug.print(language)

var greeting = "Hello from tea-lang"
var count = 2

if count == 2
  debug.print(greeting)
end

var total = 1 + 2 + 3
debug.print(total)
