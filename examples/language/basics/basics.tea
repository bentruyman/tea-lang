use debug = "std.debug"

const language = "tea-lang"
debug.print(language)

var greeting = "Hello from tea-lang"
var count = 2

if count == 2
  debug.print(greeting)
end

var total = 1 + 2 + 3
debug.print(total)

def add(a: Int, b: Int) -> Int
  return a + b
end

var x: Int? = nil
x = 1
debug.print(add(x ?? 1, 2))

var maybe_name: String? = nil
if maybe_name == nil
  maybe_name = "tea"
end

debug.print(maybe_name!)
