# Import debug helpers for console output

pub def fib(n: Int) -> Int
  if n <= 1
    return n
  end

  fib(n - 1) + fib(n - 2)
end

print(fib(30))

def func() -> Void
  if true
    print("true")
  end
end
