# Import debug helpers for console output
use debug = "std.debug"

pub def fib(n: Int) -> Int
  if n <= 1
    return n
  end

  fib(n - 1) + fib(n - 2)
end

debug.print(fib(30))

def func() -> Nil
  if true
    debug.print("true")
  end
end
