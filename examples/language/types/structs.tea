use debug = "std.debug"

struct User {
  name: String
  age: Int
}

def print_user(user: User) -> Void
  debug.print(user.name)
  debug.print(user.age)
end

var ada = User(name: "Ada", age: 37)
print_user(ada)
