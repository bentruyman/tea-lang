union Result {
  String
  Int
  Bool
}

var x: Result = "hello"

match x
  case is String => 
    @print("We have a string!")
  case is Int => 
    @print("We have an int!")
  case is Bool => 
    @print("We have a bool!")
end

if x is String
  @print("x is a String")
end
