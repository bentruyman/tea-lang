use io = "std.io"
use json = "std.json"

# Reads a JSON object from stdin and echoes a reformatted payload.

var line = io.read_line()
if line == nil
  io.write_err("no input provided\n")
  io.flush()
  return
end

var payload = json.decode(line)
io.write(json.encode(payload))
io.flush()
