use assert = "std.assert"

test "basic snapshot"
  assert.snapshot("basic", "hello snapshot\n")
end

test "empty string helper"
  assert.eq("", "")
end

test "labelled snapshot"
  assert.snapshot("with_label", "labelled output\n", "stdout")
end
