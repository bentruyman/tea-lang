<ContentPage
  eyebrow="Get Started"
  title="Installation"
  description="Install a prebuilt Tea release or build from source if you need the full development toolchain."
>
  The recommended install path downloads a prebuilt `tea` release, verifies its checksum, and installs it to `~/.local/bin`.

  ```bash
  curl -fsSL https://tea-lang.dev/install | bash
  tea --version
  ```

  Tea still needs a host C toolchain to link executables while running scripts or `tea build`:

  - macOS: `xcode-select --install`
  - Linux: install `cc` or `clang` with your package manager

  If you need a source build for local development, use the Rust/Bun/LLVM workflow instead:

  ```bash
  git clone https://github.com/bentruyman/tea-lang.git
  cd tea-lang
  ./scripts/setup-worktree.sh
  cargo build --release
  make install
  ```

  `make install` builds a bundled `tea` binary for local use. If you only run `cargo build --release -p tea-cli`, that developer build still expects `cargo` to be available when Tea compiles programs.

  If you’d rather skip the install step, you can run Tea directly from the repo during development:

  ```bash
  cargo run -p tea-cli -- examples/echo/main.tea hello tea
  ```

  <div className="divider-section" />

  <div className="flex flex-wrap gap-3 text-sm">
    <a
      href="https://github.com/bentruyman/tea-lang"
      target="_blank"
      rel="noreferrer"
      className="inline-flex items-center gap-1.5 rounded-full border border-border/70 px-4 py-2 text-muted-foreground transition-colors hover:border-primary/20 hover:text-foreground"
    >
      Repository
    </a>
    <a
      href="https://github.com/bentruyman/tea-lang/issues"
      target="_blank"
      rel="noreferrer"
      className="inline-flex items-center gap-1.5 rounded-full border border-border/70 px-4 py-2 text-muted-foreground transition-colors hover:border-primary/20 hover:text-foreground"
    >
      Report an issue
    </a>
  </div>

  <div className="divider-section" />

  <NextSteps>
    <NextLink href="/docs/getting-started" title="Getting Started" description="Run a first script and compile it." />
    <NextLink href="/docs/cli" title="CLI" description="See the supported `tea` commands and flags." />
  </NextSteps>
</ContentPage>
