<ContentPage
  eyebrow="Standard Library"
  title="std.args"
  description="Command-line argument helpers."
>
  ## Module overview

  Helpers for raw argument access, flag checks, option lookup, and positional filtering.

  <CodeCard title="Import and use" language="tea">
    {`use args = "std.args"

const verbose = args.has("--verbose")
const output = args.get("--output") ?? "result.txt"
const files = args.positional()`}
  </CodeCard>

  <StdlibFunctionPanel sourcePath="stdlib/args/mod.tea" />

  <div className="divider-section" />

  <NextSteps title="Related pages">
    <NextLink href="/examples/grep" title="grep example" description="See `std.args` used in a real CLI." />
    <NextLink href="/docs/cli" title="CLI" description="Review the `tea` command surface." />
  </NextSteps>
</ContentPage>
