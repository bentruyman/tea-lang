<ContentPage
  eyebrow="Language Runtime"
  title="Built-ins"
  description="Tea exposes a small set of global `@` intrinsics for output, script control, introspection, and math."
>
  <div className="grid gap-4 md:grid-cols-2">
    <div className="surface-quiet texture-grid-fine rounded-2xl border border-border/70 p-4">
      <p className="text-xs font-semibold uppercase tracking-[0.2em] text-primary">Output</p>
      <ul className="mt-3 space-y-2 text-sm text-muted-foreground">
        <li>`@print(value)`</li>
        <li>`@println(value)`</li>
        <li>`@eprint(value)`</li>
        <li>`@eprintln(value)`</li>
      </ul>
    </div>
    <div className="surface-quiet texture-grid-fine rounded-2xl border border-border/70 p-4">
      <p className="text-xs font-semibold uppercase tracking-[0.2em] text-primary">Program control</p>
      <ul className="mt-3 space-y-2 text-sm text-muted-foreground">
        <li>`@args()`</li>
        <li>`@exit(code)`</li>
        <li>`@panic(message)`</li>
      </ul>
    </div>
    <div className="surface-quiet texture-grid-fine rounded-2xl border border-border/70 p-4">
      <p className="text-xs font-semibold uppercase tracking-[0.2em] text-primary">Introspection and conversion</p>
      <ul className="mt-3 space-y-2 text-sm text-muted-foreground">
        <li>`@type_of(value)`</li>
        <li>`@len(value)`</li>
        <li>`@to_string(value)`</li>
        <li>`@to_float(value)`</li>
      </ul>
    </div>
    <div className="surface-quiet texture-grid-fine rounded-2xl border border-border/70 p-4">
      <p className="text-xs font-semibold uppercase tracking-[0.2em] text-primary">Math</p>
      <ul className="mt-3 space-y-2 text-sm text-muted-foreground">
        <li>`@abs`, `@sqrt`, `@floor`, `@ceil`, `@round`</li>
        <li>`@min`, `@max`</li>
      </ul>
    </div>
  </div>

  <CodeCard title="Built-ins in checked-in examples" language="tea">
    {`const all_args = @args()

if @len(all_args) == 0
  @println("")
  @exit(0)
end

@println(@type_of(all_args))
@print("ready")`}
  </CodeCard>

  <NextSteps title="Related pages">
    <NextLink href="/reference/args" title="std.args" description="Use stdlib argument helpers on top of `@args()`." />
    <NextLink href="/docs/getting-started" title="Getting Started" description="See built-ins in a first Tea program." />
  </NextSteps>
</ContentPage>
