<ContentPage
  eyebrow="Standard Library"
  title="std.json"
  description="Encode and decode JSON values."
>
  ## Module overview

  Encode Tea values as JSON and decode JSON strings back into Tea values.

  <CodeCard title="Import and use" language="tea">
    {`use json = "std.json"

const encoded = json.encode({ "name": "tea" })
const decoded = json.decode(encoded)`}
  </CodeCard>

  <StdlibFunctionPanel sourcePath="stdlib/json/mod.tea" />
</ContentPage>
