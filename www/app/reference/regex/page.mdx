<ContentPage
  eyebrow="Standard Library"
  title="std.regex"
  description="Compile regexes and transform text."
>
  ## Module overview

  Compile regex handles once, then match, replace, capture, and split text.

  <CodeCard title="Import and use" language="tea">
    {`use regex = "std.regex"

const re = regex.compile("\\d+")
const ok = regex.is_match(re, "item-42")`}
  </CodeCard>

  <StdlibFunctionPanel sourcePath="stdlib/regex/mod.tea" />

  <div className="divider-section" />

  <NextSteps title="Related pages">
    <NextLink href="/examples/grep" title="grep example" description="Real-world regex searching in Tea." />
  </NextSteps>
</ContentPage>
